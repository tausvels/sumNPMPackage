//using jquery 

//Creating a simple arithmatic function
const sum = (a,b) => {
  var result = a+b;
  console.log(result)
  return result
}

$(document).ready(()=>{
  // var input1 = window.prompt('Enter number 1:' + '')
  // var input2 = window.prompt('Enter number 2:' + '')

  // input1 = parseInt(input1);
  // input2 = parseInt(input2)

  // sum(input1, input2)
})

module.exports = sum;